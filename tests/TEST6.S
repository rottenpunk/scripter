main(argc, argv)
{

   Input = receive(Session);

   for (i = 1; i <10; i++) {
      Input = receive(Session);
      var1 = var2 + var3 / var4 + (var5 + (var6 + (var7 * var8)));
   }

   return var2;
}

